{
  "action": "RepeatWithEach",
  "params": {
    "ItemsIn": "{{VARIABLE}}"
  },
  "notes": "Inside the loop body (do), you can reference the current item with !ID:repeatItem and the index/count with !ID:repeatIndex / !ID:repeatCount.",
  "do": [
    {
      "params": {}
    }
  ]
}
