{
  "action": "RepeatWithEach",
  "params": {
    "ItemsIn": "{{VARIABLE}}"
  },
  "notes": "Inside the loop body (do), you can reference the current item with !link:repeatItem and the index/count with !link:repeatIndex / !link:repeatCount.",
  "do": [
    {
      "params": {}
    }
  ]
}
